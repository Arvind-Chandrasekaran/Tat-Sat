name: CD - Post Creation 

on:
  push:
    branches: [ main ]
    paths:
      - 'post_creation/**'
      - '.github/workflows/post_creation_cd.yml'


defaults:
  run:
    working-directory: ./post_creation

jobs:

  # This docker image is not uploaded to docker hub. Direct GitHub main branch to railway deployment is done. This image will be used in future to switch platforms. 

  build-docker:
    name: Build Production Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build -t post-creation-service:latest .


